# 글감 인박스

"왜 이러지?" 하는 순간이 전부 후보다. 다듬지 말고 한 줄씩 적립한다.
발행되면 지우지 말고 `## 발행됨`으로 옮긴다 — 시리즈 흐름이 보인다.

---

## 다음 글 (소재 정리됨)

### 계정 경계는 캐시 경계와 일치시켜라 — Claude Code 멀티 계정 3편

앞선 두 글의 결론을 겹치면 나오는 얘기.

- 공식 문서: 캐시는 **조직 단위로 격리**된다 + 같은 디렉터리 **병렬 세션은 캐시를 공유**한다
- 두 문장을 겹치면 → **같은 디렉터리의 병렬 세션을 계정으로 쪼개면 유일하게 공짜였던 캐시 공유를 잃는다**
- 반대로 워크트리·디렉터리 단위 매핑(이미 하는 방식)은 원래 캐시가 안 겹치므로 추가 비용 0
- 따라서 "세션별 계정 분리"는 직관적이지만 손해
- **계정 2개 ≠ 처리량 2배** — 한도는 덧셈으로 늘지만 파편화가 히트율을 떨어뜨린다
- 곁가지: 크레딧 구간에 들어간 계정은 TTL이 1h→5m로 떨어져 성격이 바뀐다 (연속 작업 전용으로 돌려야)
- 곁가지: 한도별 대응 순서 — Opus 한도만 걸렸으면 모델 전환, 세션/주간이면 그때 계정 전환
- 곁가지: 계정을 나누면 fork의 캐시 상속 이점을 잃는다

**선행 작업**: 아래 실측 필요

---

## 확인해볼 것 (실측 대기)

- [ ] **계정 A/B 캐시 격리 실측** — 같은 디렉터리에서 같은 프롬프트를 두 계정으로 돌려 `cache_read_input_tokens`가 0으로 나오는지. 조직 격리를 눈으로 확인하면 3편의 실측 근거가 된다
- [ ] 워크트리를 늘릴수록 캐시가 파편화되는 정도 — 실제로 얼마나 손해인지
- [ ] `/compact`를 캐시 warm/cold 상태에서 각각 돌려 비용 차이 측정

---

## 도구 (usage-statusbar 연계)

- [ ] statusline에 **계정별 캐시 히트율** 표시 — `cache_read` / `cache_creation` 비율. "이 계정은 파편화됐다"를 눈으로 보게 하는 것. 1편이 "관측 도구가 틀리면 판단이 틀린다"로 끝났으니 관측에 캐시 축을 더하는 자연스러운 속편
- [ ] 비활성 계정 숫자도 같이 표시 (1편의 "남은 것"에 적어둔 것)

---

## 블로그 운영 자체

- [ ] **GitHub Actions 빌드 전환기** — 플러그인 화이트리스트를 벗어나는 과정. 왜 필요했고 무엇이 열렸는지
- [ ] OG 이미지 자동 생성 파이프라인
- [ ] 이 블로그의 조판을 고친 이야기 — 제목 중복 하나가 목록 발췌문까지 망가뜨리고 있었던 것. 관측/디버깅 서사로 쓸 수 있다

---

## 짧은 글 후보

10분짜리를 매번 쓰려 하면 지속되지 않는다. 아래는 1500자 이하로.

- [ ] Claude Code 업데이트에서 바뀐 것 하나씩
- [ ] `/rewind`가 `/compact`보다 싼 이유 (긴 글에서 떼어내 독립시켜도 됨)
- [ ] worktree마다 캐시가 따로 쌓인다는 사실 하나만

---

## 발행됨

- 2026-08-01 [Claude Code 토큰, 어디서 새고 있나](https://redlasha.github.io/software/claude-code-token-saving/) — 캐시 무효화 지점과 컨텍스트 관리
- 2026-07-31 [클로드 계정 두 개 쓰다가 발견한, 거짓말하는 statusline](https://redlasha.github.io/software/claude-two-accounts/) — 멀티 계정 관측 문제, 역할 분리
- 2026-03-09 [The Universal Loop](https://redlasha.github.io/software/the-universal-loop/) — 관찰·이해·행동
